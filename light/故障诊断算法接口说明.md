# 故障诊断算法接口说明

平台封装已迁移至项目根目录 **`platform/`**。

请阅读：

- [platform/README.md](../platform/README.md) — 三层结构与调用链路（**组装平台必读**）
- [platform/故障诊断算法接口说明.md](../platform/故障诊断算法接口说明.md) — 训练/测试接口详情

**测试时 `run_dir` 应指向 `outputs/diagnosis/` 下的正式实验目录**（含 `checkpoint_best.pth` 与 `config_resolved.yaml`），不要使用冒烟训练目录。
