# 数据增强算法接口说明

平台封装已迁移至项目根目录 **`platform/`**。

请阅读：

- [platform/README.md](../platform/README.md) — 三层结构与调用链路（**组装平台必读**）
- [platform/数据增强算法接口说明.md](../platform/数据增强算法接口说明.md) — 训练/生成接口详情

**生成时 `run_dir` 应指向 `outputs/data_aug/` 下的正式实验目录**（含 `checkpoint_best.pth` 与 `config_resolved.yaml`）。
